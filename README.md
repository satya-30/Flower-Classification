# Flower-Classification
This project demonstrates flower classification using transfer learning with TensorFlow and PyTorch. Pre-trained CNNs (MobileNet, ResNet) are fine-tuned on the Flowers Recognition dataset. Includes model training, evaluation, and performance visualization for five flower classes.
